# CLAUDE.md

Bevorzuge funktionalen Stil. Vermeide Klassen, außer das Framework erzwingt
sie (React Error Boundaries, Drizzle Relations). Reine Funktionen sind das
Default.

Tests liegen direkt neben der Quelldatei: `foo.ts` plus `foo.test.ts`.
