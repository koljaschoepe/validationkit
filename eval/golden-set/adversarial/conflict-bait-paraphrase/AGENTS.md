# AGENTS.md

Use functional programming style. Avoid classes unless the framework demands
one (React error boundaries, Drizzle relations). Prefer pure functions.

Tests live next to the source file: `foo.ts` + `foo.test.ts`.
