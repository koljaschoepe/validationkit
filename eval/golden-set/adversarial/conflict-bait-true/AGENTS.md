# AGENTS.md

Indentation: use **tabs**. Width: 4 spaces equivalent.
Pre-commit: run `pnpm format` which enforces tabs.

When editing TS files, never reformat to spaces — the CI lint will block the
PR. If you see space-indented code in a diff, normalize to tabs before pushing.
