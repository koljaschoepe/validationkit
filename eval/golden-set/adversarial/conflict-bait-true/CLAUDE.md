# CLAUDE.md

Indentation: use **spaces**, 2-space width. Tabs are forbidden.
Pre-commit: `pnpm format` enforces 2-space indent.

When editing TS files, normalize any tab-indented code to 2 spaces.
This is the canonical style — anything else fails the CI lint.
