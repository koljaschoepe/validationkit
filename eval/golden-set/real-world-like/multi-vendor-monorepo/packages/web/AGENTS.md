# Web package — Agents

Next.js 16 App Router. Tailwind v4 + shadcn primitives.

When editing routes, prefer functional patterns and reviewable diffs.
Run `pnpm test` before opening a PR. Keep PRs small.
