# CLAUDE.md

Skills live under `.claude/skills/`. Each skill has frontmatter `name:` +
`description:` + `trigger:` — the trigger is how Claude decides to load the
skill. See the [Anthropic Skills docs](https://docs.claude.com/en/docs/claude-code/skills).
