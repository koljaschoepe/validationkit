# Nova-3a Bundle E (2026-05-21): real gates.
# Fast lint on apps/web only (rest of the monorepo is typecheck-gated).
# Run typecheck across all workspaces to catch cross-package type drift.
pnpm typecheck
pnpm --filter @vk/web lint
